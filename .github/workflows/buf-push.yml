name: Buf Push
on:
  push:
    branches:
      - main
jobs:
  buf-push:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: bufbuild/buf-setup-action@v0.6.0
      - name: Buf Login
        run: echo ${{ secrets.BUF_TOKEN }} | buf registry login --username=emilbon99 --token-stdin
      - name: Buf Push Cluster
        run: buf push pkg/cluster --verbose --debug
      - name: Buf Push API
        working-directory: pkg/api/rpc
        run: basch -c "git checkout main && buf push"



